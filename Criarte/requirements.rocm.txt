# AMD ROCm environment dependencies (keep requirements.txt for NVIDIA/CUDA)

# Ultralytics and CV stack
ultralytics==8.4.15
opencv-python-headless==4.13.0.92
matplotlib==3.10.8
PyQt5==5.15.11
onnxruntime==1.24.2
numpy==2.4.2

# Important:
# - Install ROCm-enabled PyTorch and torchvision from your OS packages (Arch):
#   sudo pacman -S --needed python-pytorch-rocm python-torchvision rocminfo rocm-hip-runtime
# - Then create a venv with --system-site-packages so it can use the ROCm torch libs.
